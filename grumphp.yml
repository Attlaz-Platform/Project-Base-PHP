parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpstan:
            level: 7
            ignore_patterns: ["*Test.php"]

        phpcs:
            standard: PSR2
            ignore_patterns: ["*Test.php"]
