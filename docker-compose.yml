version: '3'

services:
  composer:
      image: composer:latest
      volumes:
            - .:/app

      #docker-compose run composer update --working-dir . --ignore-platform-reqs

  phpunit:
        image: phpunit/phpunit:6.0.6
        volumes:
              - ../:/app
        #docker-compose run phpunit -c /app/phpunit.xml

